{
  "name": "backend-trello-app",
  "version": "1.0.0",
  "description": "Backend project to send data information to front",
  "main": "index.js",
  "scripts": {
    "dev": "set DEBUG=app:* && nodemon index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Juan Ayala",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
